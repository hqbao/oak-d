depthai>=3.6
numpy>=2.0
# cv2 is NOT in the FLIGHT runtime any more -- the whole imu_camera -> vio -> slam
# path (incl. --vl53l9cx ToF + --direct dense VO + ORB loop closure) runs with
# opencv-python uninstalled (see requirements-flight.txt and
# verification/cv2_absent_flight_litmus.py). It is kept here for DEV/TOOLING only:
# the Qt UI overlays (ui/viz/*), the camera-calibration wizard (sky/calib/*,
# --use-camera-calib), the dev-only PnP A/B oracle (OAKD_OWN_PNP=0), and the
# parity/oracle/bench tooling.
opencv-python>=4.10
pyqtgraph>=0.13
PyQt6>=6.6
PyOpenGL>=3.1
pyserial>=3.5
# Optional: JIT-accelerates our own pure-NumPy optical flow (ours/vio/klt.py) so
# the library-free frontend runs in real time (~15ms/frame vs ~140ms without).
# Not required -- klt.py falls back to the pure-NumPy path when numba is absent.
numba>=0.61
